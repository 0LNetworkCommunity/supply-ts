{
  "name": "supply",
  "module": "index.ts",
  "devDependencies": {
    "@biomejs/biome": "1.5.2",
    "@types/bun": "latest",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "start": "bun run main.ts",
    "lint": "biome lint . --apply",
    "format": "biome format . --write && biome lint . --apply"
  },
  "type": "module",
  "dependencies": {
    "axios": "^1.6.5"
  }
}