{
  "name": "supply",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "format": "biome format . --write && biome lint . --apply"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.2",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}